---
name: OpenVox
icon: '<svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><ellipse cx="24" cy="24" rx="18" ry="7"/><ellipse cx="24" cy="24" rx="18" ry="7" transform="rotate(60 24 24)"/><ellipse cx="24" cy="24" rx="18" ry="7" transform="rotate(120 24 24)"/><circle cx="24" cy="24" r="2.6" fill="currentColor" stroke="none"/></svg>'
order: 1
eyebrow: Core Platform
tagline: The engine itself
docsPath: /openvox/latest/
title: "OpenVox — OpenVox"
description: "OpenVox is the community implementation of Puppet — the agent and catalog compiler that keeps every managed node in its declared state. It runs standalone or paired with OpenVox Server, and every manifest, module, and Hiera dataset you already have carries over unchanged."
pageId: products
features:
  - heading: Cross-platform
    body: Manages Linux, Unix, and Windows systems from a single declarative language.
  - heading: Agent or standalone
    body: Run with a central server for fleet-wide management, or standalone for local, agentless runs.
  - heading: Drop-in compatible
    body: Existing manifests, modules, Hiera data, and tooling transition from Puppet Open Source without changes.
  - heading: Secure by default
    body: Agent-to-server traffic runs over HTTPS with mutual TLS.
---
OpenVox is the community implementation of Puppet — the agent and catalog compiler that keeps every managed node in its declared state. It runs standalone or paired with OpenVox Server, and every manifest, module, and Hiera dataset you already have carries over unchanged.
